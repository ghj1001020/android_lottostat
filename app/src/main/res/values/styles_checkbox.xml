<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 체크박스 -->
    <style name="BaseCheck" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:textColor">@color/txt_black</item>
        <item name="android:textSize">16dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/chk_default</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="BonusCheck" parent="BaseCheck">
        <item name="android:background">@drawable/chk_bonus</item>
    </style>

    <style name="FoldCheck" parent="BaseCheck">
        <item name="android:background">@drawable/chk_arrow</item>
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
    </style>

</resources>